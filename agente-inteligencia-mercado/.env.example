# Copia este ficheiro para ".env" (na mesma pasta) e preenche com a tua chave real.
# NUNCA commitar o ficheiro ".env" — já está listado no .gitignore da raiz do repositório.

# Chave única da AIsa (Model Gateway + APIs/dados + Agent Skills).
# Obter em: https://aisa.one (painel da conta)
AISA_API_KEY=
